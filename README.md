# Local Relayer

Local Relayer = LocalOsmosis + LocalJuno + Relayer

## Start

```sh
make start
```

Stop (and retain chains data:

```sh
make stop
```

Stop and delete chains data:

```sh
make restart
```
